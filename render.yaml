services:
  - type: web
    name: vin-sight
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: NODE_ENV=production node --import tsx/esm server.ts
    envVars:
      - key: NODE_ENV
        value: production
